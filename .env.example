# Azure Storage — Portal: Storage account → Security + networking → Access keys → Connection string
# Account: hellocarl2026 (use the full primary connection string)
AZURE_STORAGE_CONNECTION_STRING=

# Optional blob container name (default: hellocarl-data). Created automatically if missing.
# AZURE_STORAGE_CONTAINER=hellocarl-data
